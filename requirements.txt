# RAKAN - Python Dependencies
# Core dependencies for RAKAN

# Configuration management
PyYAML>=6.0

# Logging
colorlog>=6.0  # For colored console output

# Platform detection
psutil>=5.9.0  # For hardware detection and system monitoring

# Async support
aiofiles>=23.0.0  # For async file operations

# HTTP client (for downloads)
aiohttp>=3.8.0  # For async HTTP requests

# Progress bars
tqdm>=4.65.0  # For progress indication

# Backend API (optional - for web server)
fastapi>=0.100.0  # For HTTP API server
uvicorn>=0.23.0  # For ASGI server
websockets>=11.0  # For WebSocket support

# Testing dependencies (dev)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0